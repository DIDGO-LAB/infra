USE user_db;

-- user_db schema is owned by user-service Flyway migrations.
-- Keep this init file minimal so infra does not drift from the service source of truth.

USE training_db;

-- training_db schema is owned by training-service Flyway migrations.
-- Keep this init file minimal so infra does not drift from the service source of truth.
